// Generated by CoffeeScript 1.4.0
(function() {
  var myCategory, myJson, myList, myObject, mySearch;

  myList = new LOAF.YelpList;

  mySearch = new LOAF.YelpList([], {
    term: "cookies"
  });

  myCategory = new LOAF.YelpList([], {
    category: "active"
  });

  myList.fetch();

  myList.fetch({
    page: 2
  });

  myList.models;

  myList.size();

  myList.size();

  myList.fetch({
    page: 2
  });

  myList.size();

  myObject = {
    a: 12,
    b: 14,
    c: 16
  };

  myJson = new LOAF.FsJsonObject({
    read: false,
    onReady: function(fsObject) {
      console.log("Ready to go");
      return jsObject.writeObject(myObject, function() {
        var newJsonObject;
        console.log("Write success.");
        return newJsonObject = new LOAF.FsJsonObject({
          read: true,
          onReady: function(newFsObject) {
            return console.log(fsObject.getObject());
          }
        });
      });
    }
  });

  myJson = new LOAF.FsJsonObject({
    read: true,
    size: 5120,
    fileName: "Breadcrumbs.txt",
    onReady: function() {}
  });

}).call(this);
